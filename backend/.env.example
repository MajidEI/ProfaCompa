# =============================================================================
# Salesforce Profile Compare - Backend Environment Variables
# =============================================================================

# Salesforce Connected App Credentials
# Create a Connected App in Salesforce Setup > App Manager
SF_CLIENT_ID=your_connected_app_client_id
SF_CLIENT_SECRET=your_connected_app_client_secret

# OAuth Callback URL
# LOCAL:      http://localhost:3001/auth/callback
# PRODUCTION: https://your-backend.onrender.com/auth/callback
SF_CALLBACK_URL=http://localhost:3001/auth/callback

# Session secret for Express sessions (use a strong random string in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-secure-session-secret-change-this

# Server configuration
PORT=3001

# Frontend URL(s) for CORS
# Supports multiple URLs separated by commas
# LOCAL:      http://localhost:5173
# PRODUCTION: https://your-app.vercel.app,https://your-custom-domain.com
FRONTEND_URL=http://localhost:5173

# Salesforce login URL
# Production orgs: https://login.salesforce.com
# Sandbox orgs:    https://test.salesforce.com
SF_LOGIN_URL=https://login.salesforce.com

# =============================================================================
# Production Deployment Notes:
# =============================================================================
# 1. Update SF_CALLBACK_URL to your deployed backend URL
# 2. Update FRONTEND_URL to your Vercel deployment URL
# 3. Generate a secure SESSION_SECRET
# 4. Update your Salesforce Connected App callback URL to match SF_CALLBACK_URL
# =============================================================================
