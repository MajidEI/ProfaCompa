# Frontend Environment Variables
# For Vercel deployment, no environment variables are needed in frontend
# The API routes are served from /api/* on the same domain

# For local development with separate backend, you can override:
# VITE_API_URL=http://localhost:3001
